# This module is just a small demo if you desire to develop your modules with
# Elixir.
# You have to create a file with the .ex extension and this one will be compiled
# via the Makefile.
defmodule RCouch.Elixir.Demo do
  def init(args) do
    IO.inspect args
  end
end
